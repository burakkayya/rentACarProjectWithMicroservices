spring:
    datasource:
        password: 12345
        url: jdbc:postgresql://localhost:5432/inventorydb
        username: postgres
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
            javax:
                persistence:
                    validation:
                        mode: none
